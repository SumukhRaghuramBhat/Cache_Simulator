#include<stdio.h>

#define M 5000
#define N 1000

void main(void)
{
    long array[M][N] = {0}, i = 0, j= 0, k = 0;
    
        for(j = 0; j< M; j++)
        {
            for(i = 0; i < N; i++)
            {
                arr[i][j] += 2;
            }
        }
}
